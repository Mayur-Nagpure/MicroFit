#server:
#  port: 8888
#
#spring:
#  application:
#    name: config-server
#  cloud:
#    config:
#      server:
#        git:
#          uri: https://github.com/Mayur-Nagpure/config-server-properties
#          default-label: master
#          clone-on-start: true
#          search-paths: .





server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
      active:
        - native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config